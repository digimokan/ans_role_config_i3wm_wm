# command to launch the screen-lock application
lock_screen_cmd: 'i3lock'

# keymap to launch the screen-lock application
keymap_lock_screen: '$mod+Shift+l'

# keymap to exit i3wm
keymap_exit_i3wm: '$mod+Control+e'

